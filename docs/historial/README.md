# Historial de trabajo (Cursor)

Los chats de Cursor viven en la carpeta del workspace, no en git. Al abrir `D:\GITHUB\BARCODE` se crea un chat nuevo; el hilo largo de barcode4.0 queda en el proyecto original.

Copias de las conversaciones (para no perder el contexto):

- `barcode4.0-principal.jsonl` — hilo principal (`z:\barcode4.0`): ZPL, cola #21, editor JSON, formato 13/GTIN, sync Odoo/FM, caducidad `exp`, migración a esta carpeta.
- `dropbox-barcode4.0.jsonl` — hilo anterior (`z:\dropbox\barcode4.0`): sync Odoo/`itemsM.json`, `.env`, días de vencimiento en `items.exp`.

Original: `z:\barcode4.0` (no se borró).
Este home: `D:\GITHUB\BARCODE` — arrancar con `start.bat`.
