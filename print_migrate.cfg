# Impresoras que usa el servicio NUEVO (cola isabel_zpl_queue).
# El worker en la PC de impresoras las toma todas.
# Web/API (Servicios) genera el ZPL; el worker solo imprime RAW.
#
# Formatos:
#   GK420t_chica   → 1, 9, 15, 21
#   GK420t_grande  → 5
#   GK420t_3x1.25  → 10 / 14 (BMP en printserver/)
#   GK420t_3x2     → 10 / 14 (BMP) + GTI 13 caja Rey (export_code13)
#
# Formato 13 sigue por IP (print_ip_13.cfg), no por esta cola.

GK420t_chica
GK420t_grande
GK420t_3x1.25
GK420t_3x2
